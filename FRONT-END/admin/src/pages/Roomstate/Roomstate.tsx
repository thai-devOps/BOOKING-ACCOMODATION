import React from 'react'
// Define props interface used
interface IRoomstateProps {}
const Roomstate: React.FC<IRoomstateProps> = (_props: IRoomstateProps) => {
  return <div>Roomstate</div>
}
export default Roomstate
